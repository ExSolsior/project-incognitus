[package]
name = "project-incognitus"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
bytemuck = { workspace = true }
pinocchio = { workspace = true }
# my-lib = { path = "../lib" }

[dev-dependencies]
mollusk-svm = { workspace = true }
